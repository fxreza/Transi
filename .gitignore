# SwiftPM build products
.build/
build.noindex/

# Local code-signing material (see README > "Permissions survive rebuilds").
# The certificate is per-machine; each developer creates their own.
certs/

# macOS
.DS_Store

# Editor / tooling state
.claude/

# Claude Code project instructions (kept local, not published)
CLAUDE.md

# API keys for local testing (never commit)
.env
